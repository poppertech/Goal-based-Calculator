CREATE TABLE [Simulation].[ForecastType] (
    [Id]           INT          NOT NULL,
    [ForecastType] VARCHAR (16) NOT NULL,
    CONSTRAINT [PK_Simulation]].[ForecastType] PRIMARY KEY CLUSTERED ([Id] ASC)
);

